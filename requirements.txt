Flask==3.1.3
Werkzeug==3.1.8
gunicorn==23.0.0
openpyxl==3.1.5
Pillow==12.2.0
# Solo se importan (import perezoso) si DATABASE_URL / AWS_S3_BUCKET están
# configuradas — ver README, sección "Base de datos persistente en AWS
# (RDS + S3)". No hacen falta para correr localmente con SQLite/disco local.
psycopg2-binary==2.9.9
boto3==1.34.162
